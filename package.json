{
  "name": "zone-x",
  "private": false,
  "version": "0.16.0",
  "type": "module",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "clean": "rm -rf dist",
    "build": "bun run clean && tsc && tsup --dts",
    "publish": "bun run build && npm publish",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.2.2",
    "vite": "^5.0.0",
    "vitest": "^1.3.1"
  }
}
